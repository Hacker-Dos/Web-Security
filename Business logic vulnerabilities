                      Business logic Vulnerabilities

# Business logic vulnerabilites are flaws in how application's rules and workflows are designed allowing attackers to misuse legitimate functionlaity for malicious purposes.

# what are Business logic vulnerabilites ?
- Defination : Errors in the application's logic that let attackers bypass intended workflows or constraints
- Nature : The don't exploit coding bus like sql injeciton ; instead , they exploit how the system is supposed to work
- Other names : Application logic flaws or simply logic flaws

# How they Arise >
- Flawed assumptions
- Weak validation
- Complex systems
- Poor documentaion


# Impact 
- Authentication flaws
- Financial manipulation
- Business disruption
- Broad severity range


# Excessive trust in client-side controls

1. Excessive trust in client-side controls
  - solution : capture the  Post /cart request at the end price parameter is there try to manipulate it and set price to less than given credit

2. 2FA broken logic 
  - solution : log in with give creds capture the traffic  notice that POST /login2 request has the verify parameter  remove the session  in verify parameter change it ot carlos  send the GET /login to get the carlos a mfa code  after sending the code go to the post request make the bruteforce the mfa code get the session id in response and login 


# Failing to handle unconventional input

1. High-level logic vulnerability
     - solution : In the POST /cart  at the end the quantity parameter is there  make it to negative value (-2,-3) buy the leather jacket

2. Low-level logic flaw
  - solution : Making interget overflow here get the  POST /cart request in the last quantity parameter  make it to 99 and give null payloads and continue indefinitely option check the value is getting negative and make calculation based on that add other items to manage and done

3. Inconsistent handling of exceptional input
  - solution :  After 255 characters they will get truncated the excat mail i used this is the payload(AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAADontWannaCry.com@exploit-0a7200e204f51e738095c6f401b70005.exploit-server.net)

# Making flawed assumptions about user behavior

 -Tusted users won't always remian trustworthy
  
1.Inconsistent security controls
 -solution : create an account with the mail given in exploit email server confirm registration Now create now there is email update functionality update the email with something@dontwannacry.com delete the carlos solved


# User won't always supply mandatory input

1. Weak isolation on dual-use endpoint
 -solution : In this lab capture the POST /my-account/change-password request then try to remove the current-password parameter and see if password is getting changed then change username to administrator and then password to test done login and delete carlos

2. Password reset boken logic
  -soltuion : click on the forgot password and then send it email click email link and update the new password then we will get the  POST /forgot-password?tem- request at the end try to change the name of the user in the parameter done show response in browser and login in with update based and carlos account


# User won't always follow the intended sequence

1. 2FA simple bypass
  -solution : Enter the credentials of victim intercept it send the and then GET /login2 request is there drop that request and in the url change it /my-account in the path and now off the intercept check the browser

2. Insuffcient workflow validation 
  -solution : first purchase the item with suffcient funds see the request GET /cart/order-confirmation then go to cart and add the leather jacker to cart and make the same GET request check in the browser

3. Authentication bypass via flawed state machine
  -solution : login with the given credentials send the request with credentials and drop the role-selector request in the browser remove the role-selector and finally reload the browser the admin panel will be there delete carlos


# Domain-specific flaws 

  1. Flawed enforcement of business rules
      -solution : In the home page at the end there is sign up to newletter so in that add dummy mail to get new coupen code then use both coupen code alternative to get discount until we buy the leather jacket

  2. Infinite money logic flaw
    -solution :   - Sign up for the newsletter to get coupon SIGNUP30, buy a $10 gift card, apply the 30% discount, and copy the generated code.
                  - Redeem the gift card via POST /gift-card with the code, adding $3 store credit.
                  - In Burp, create a Session handling rule with a macro: POST /cart → POST /cart/coupon → POST /cart/checkout → GET /cart/order-confirmation → POST /gift-card.
                  - Configure the macro so the gift-card parameter in POST /gift-card is derived from the order-confirmation response.
                  - Launch a Burp Intruder Sniper attack on /my-account with 412 null payloads, single-threaded, to accumulate enough credit to buy the jacket.

# Providing an encryption oracle

1. Authentication bypass via encryption oracle
 - solution : kindly follow the solution by the community provided


# Email address parser discrepancies

1. Bypassing access controls using email address parsing discrepancies
 - solution : {=?utf-7?q?admin&AEA-exploit-0a8b0095034bdd4680844dc301f100b6.exploit-server.net&ACA-?=@ginandjuice.shop} replace the email server domain and register and confirm the registration and delete the carlos















 















